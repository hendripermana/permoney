# Local development overrides
# ------------------------------------------------------------------------------
# Copy this file to `.env.local` so Dotenv overrides the production defaults in
# `.env`.  NEVER point local commands at the production database.

RAILS_ENV=development
RACK_ENV=development

# Use isolated Postgres databases for development + tests
POSTGRES_DB_DEVELOPMENT=permoney_development
POSTGRES_DB_TEST=permoney_test
POSTGRES_DB=permoney_development

# Point to localhost when running Rails outside of Docker
DB_HOST=127.0.0.1
DB_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Non-production Redis instance
REDIS_URL=redis://127.0.0.1:6379/1

# Development friendly defaults
SELF_HOSTED=false
ONBOARDING_STATE=open
AI_DEBUG_MODE=false

# Provider + AI keys can be set here if you need them locally
TWELVE_DATA_API_KEY=
OPENAI_ACCESS_TOKEN=
OPENAI_URI_BASE=
OPENAI_MODEL=

# (example: LM Studio/Docker config) OpenAI-compatible API endpoint config
# OPENAI_URI_BASE=http://host.docker.internal:1234/
# OPENAI_MODEL=qwen/qwen3-vl-4b

# OpenID Connect for development
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_ISSUER=
OIDC_REDIRECT_URI=http://localhost:3000/auth/openid_connect/callback

# Langfuse config (optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com
