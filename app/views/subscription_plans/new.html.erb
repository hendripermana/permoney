<div class="container mx-auto px-4 py-6">
  <div class="mb-8">
    <nav class="flex items-center gap-2 text-sm text-secondary mb-4">
      <%= link_to "Subscriptions", subscription_plans_path, class: "hover:text-primary" %>
      <%= icon "chevron-right", class: "h-4 w-4" %>
      <span class="text-primary">New Subscription</span>
    </nav>

    <div class="flex items-center gap-3">
      <%= icon "plus-circle", class: "h-8 w-8 text-blue-600" %>
      <div>
        <h1 class="text-2xl font-bold text-primary">Add New Subscription</h1>
        <p class="text-secondary">Track a new subscription or recurring payment</p>
      </div>
    </div>
  </div>

  <div class="max-w-3xl">
    <%= render "form" %>
  </div>
</div>
