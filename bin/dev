#!/usr/bin/env bash

# Development process manager
# Runs web server, CSS watcher, and Sidekiq worker

if ! command -v foreman &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi

# Use foreman to run all services from Procfile.dev
exec foreman start -f Procfile.dev "$@"
